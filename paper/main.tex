% =====================================================================
%  gust2 — TMLR submission scaffold
%  Compile (anonymous submission):   pdflatex main ; bibtex main ; pdflatex main ; pdflatex main
%  Camera-ready (reveal authors):    \usepackage[accepted]{tmlr}
%  De-anonymized preprint (arXiv):   \usepackage[preprint]{tmlr}
%  Style files (tmlr.sty/.bst, fancyhdr.sty, math_commands.tex) are vendored in this dir.
%  Structure + per-section contents are specified in OUTLINE.md — this file is the skeleton only.
% =====================================================================
\documentclass{article}
\usepackage{tmlr}                 % anonymous double-blind submission by default
% \usepackage[accepted]{tmlr}     % <- swap in for the camera-ready
% \usepackage[preprint]{tmlr}     % <- swap in for a de-anonymized preprint

\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage{subcaption}
\input{math_commands.tex}         % optional TMLR math macros

% Pull figures straight from the existing analysis layer (no copying needed yet).
\graphicspath{{../plots/}{../presentation/figures/}{../}}

\title{Structurally Stable Turbulence Rollouts via Discrete Next-Scale Prediction}
% ^ WORKING TITLE — claims-style, not novelty-style (TMLR judges claims-vs-evidence, not novelty).

% Author block is auto-anonymized under \usepackage{tmlr}; real names appear only with [accepted].
\author{\name Anonymous \email anon@example.com \\
        \addr Anonymous Institution}

\begin{document}
\maketitle

\begin{abstract}
% ~150-200 words. State: (i) long-horizon (post-Lyapunov) statistical stability — not short-term
% RMSE — is the right success criterion for chaotic-turbulence surrogates; (ii) discrete next-scale
% prediction over a multi-scale VQ codebook stays on-manifold for 2000 steps with FAR LESS
% intervention than continuous AR; (iii) the load-bearing fix is a per-TOKEN (not per-scale) support
% constraint; (iv) headline secondary findings: data-limited compute-optimum D/P ~= 0.54, below-floor
% EMD, the climate-vs-forecast temperature split. Lead with claims you can back with evidence NOW.
TODO.
\end{abstract}

% =====================================================================
\section{Introduction}
\label{sec:intro}
% - Frame the metric first: chaotic systems decorrelate past the Lyapunov time, so short-term RMSE is
%   the wrong bar; staying on the attractor (statistical/spectral fidelity) for 2000 steps is the bar.
% - The central rhetorical move: structural vs numerical stability. Discrete-token AR is structurally
%   on-manifold at the MARGINAL level (codebook + per-position support); continuous AR stability is a
%   tuned numerical margin (noise schedule / pushforward / refinement steps). Different CATEGORIES.
% - Numbered headline claims a reader can latch onto (Llama-report style preview). See \ref{sec:claims-note}.
% - Honesty up front: "less intervention," NOT "stable by construction" / "for free" (the model DID
%   collapse before the fix).

% Numbered-claims preview list (the spine; full claim<->evidence map is in OUTLINE.md):
% C1  Two distinct rollout collapse modes (positional-OOD vs diffusive) with OPPOSITE logit signatures.
% C2  In VAR-style parallel scale emission the constraint must be PER-TOKEN, not per-scale; the
%     per-token CE mask (+substitution noise) takes survival from 0-4% to 88-100%.
% C3  Spectral bias is RELOCATED (paid once in the tokenizer), not removed: discrete latent AR avoids
%     compounding it; continuous pixel/latent AR does not.
% C4  Per-position OOD-emission rate is a discrete-native rollout-health alarm; spectra are the primary
%     stability metric; EMD is necessary-but-insufficient.
% C5  Compute-optimal scaling for an AR-token loss on a PDE dataset: D_uniq/P ~= 0.54 (~20x tighter
%     than text); below-floor EMD on sc341 via mode-averaging.

% =====================================================================
\section{Problem Setup, Data, and Evaluation Protocol}
\label{sec:setup}
% - 2D forced turbulence (vorticity omega), ~20k 256x256 frames; train/val split; normalization; the
%   choice of evaluation window.
\subsection{Success criterion and the metric hierarchy}   % (supports C4)
% - Define the battery IN ORDER OF EVIDENTIAL WEIGHT and commit to it before showing any result:
%   (1) time-averaged + time-resolved spectra (primary), (2) per-position OOD-emission rate
%   (discrete-native alarm), (3) snapshots, (4) EMD / pixel histograms (FIDELITY ONLY — over-flags).
% - State plainly: no clean OOD/off-manifold detector exists in general (joint-config membership is
%   intractable) — a property of the problem, not a gap. Do NOT use collapse_rate as THE metric.

% =====================================================================
\section{Multi-Scale VQ Tokenizer}
\label{sec:tokenizer}
% - ViT encoder/decoder (SwiGLU, RMSNorm, 2D RoPE, QK-norm); multi-scale residual VQ over a SINGLE
%   SHARED codebook held as external EMA state; compound loss; bf16/f32 mixed precision.
% - Codebook sweep -> dim=512, size=4096, beta=0.25, ema_decay=0.90. Scale configs: sc341/sc917/sc1941.
% - DETERMINISTIC quantization (nearest-neighbor argmax, vq.py:33; no stochastic/Gumbel path) -- a deliberate
%   tuning-variable reduction with a spectral cost (high-k tail flattening = the once-paid bias).
% - VQ-VAE SIZE barely moves the floor: round-trip EMD saturates by Medium, non-monotone (Large can be worse:
%   sc917 0.106->0.116; sc1941 Medium 0.055 best). Scale COUNT is the lever (sc341->sc1941 at fixed Small drops
%   the floor ~5x). Codebook only ~59% utilized, 0 dead codes -> floor is scale-resolution + det-argmax bound,
%   NOT enc/dec-capacity or codebook-capacity bound. Justifies a small/medium VQ. (FIG: codebook_analysis/, compare_recon.png)
% - FAILED-EXPERIMENT writeup (this genre rewards it): DiVeQ + Product SF-DiVeQ both collapse to the
%   trivial-reconstruction attractor (val MSE ~84); EMA retained.
% FIG: ../plots/vqvae_arch.png, ../plots/multiscale_vq.png, ../plots/codebook_analysis/*

% =====================================================================
\section{Next-Scale Prediction Model}
\label{sec:nsp}
% - VAR-style teacher-forced AR transformer; unified effective_vocab heads (no per-scale offsets).
% - ExpansionHeads + shared within-scale refinement stack (the fine-scale-CE fix; pre/post-merge
%   ablation). Attention mask: t0->t0 full, t0->t1 blocked, t1->t0 full, t1->t1 scale-causal.
% - NOTE: per-scale loss weight in code is 1/log(token_count+1) normalized to mean 1 (CLAUDE.md /
%   nsp_model.py docstring say 1/sqrt — fix the docs before quoting a formula).
% FIG: ../plots/nsp_arch.png, ../plots/attention_mask.png

% =====================================================================
\section{Rollout Collapse: Two Failure Modes and the Per-Token Recipe}
\label{sec:recipe}
% *** THE METHODS ANCHOR. This is a mechanism paper, not a benchmark paper. ***  (supports C1, C2)
% Tell it as the honest experiment arc (reuse the Test 1->8 backbone from nsp_stabilization_report.tex,
% but with the corrected metric framing):
%   1. Collapse observed; pixel-EMD-to-GT as a coarse detector.
%   2. Sampling-only fixes ruled out (temperature/top-k/top-p keep 0 trajectories alive) -> structural.
%   3. Diagnosis: per-position OOD-emission rate spikes ~2% -> ~28% in lockstep; "confident but
%      confidently OOD" (no logit precursor).
%   4. THE INSIGHT: per-position support is wildly non-uniform; a per-SCALE mask leaves low-support
%      cells unconstrained -> collapse. The constraint is PER-TOKEN.
%   5. Train-time per-token CE mask + 10% same-position substitution noise (replacement, not nulling).
%   6. Result: 0-4% -> 88-100% survival.
% - Distinguish the two collapse modes explicitly (C1): positional-OOD (entropy UP, off-manifold,
%   fixed by mask) vs diffusive/mean-reversion (entropy cliff DOWN + confidence UP, in-distribution,
%   fixed by temperature, bounded by the tokenizer's spectral floor [scale-resolution + det-argmax, NOT codebook
%   capacity -- only ~59% utilized]). CONFLATING THEM IS THE CENTRAL RISK.
% - Substitution noise is SECONDARY in the telling (don't let it get conflated with pushforward).
% NEEDS (P0-2): one clean 3-way ablation {no-mask / per-scale / per-token} at a flagship config,
%   n_traj>=8, OOD-rate trace overlaid. Current evidence is scattered heterogeneous April runs.
% FIG: position_ood_overlay.png, logits_aligned_overlay.png, posmask_multitraj_grid.png,
%      robust_survival_curves.png, robust_emd_traces.png, robust_multitraj_grid.png

% =====================================================================
\section{Why Discrete Is Structurally Stable: Spectral Bias Relocated}
\label{sec:spectral}
% (supports C3 — the STRONGER pillar, but currently the LESS-MEASURED one.)
% - Argument: spectral bias is conserved (NN+backprop). Pixel-AR pays it as compounding high-k blur
%   (PDE-Refiner's diagnosis). Continuous-LATENT AR still compounds it in latent space (NOT free).
%   Discrete latent ROUTES it off the spectral axis: codebook = turbulent basis w/ a CONDITIONAL
%   floor; argmax/sampling = high-k-restoring nonlinearity. The floor is CONDITIONAL/APPROXIMATE
%   (bounds marginal not joint) — that is WHY the per-token mask (Sec \ref{sec:recipe}) ENABLES it.
% - Baseline suite (BASELINES_SPEC.md). All four are SPECCED, not yet run:
%     B1 skip-quantization continuous-latent MSE  -> the clean isolation of the discrete axis (P1-6).
%     B2 pixel-space U-Net trio (MSE / +noise / +pushforward) -> recognizable PDE-community anchor.
%     B3 flat-VQ tokenizer Pareto (GATE: run FIRST; if flat-1024 floor ~ sc917 floor, this story dies).
%     B4 no-training controls (persistence, in-mask random, VQ floor) -> bracket: random <= AR <= floor.
% - CRITICAL EXPOSURE: today the discrete-vs-continuous claim is demonstrated BY CITATION; B1 converts
%   it to a measured result. This is the biggest reviewer-risk under TMLR criterion 1.
% FIG: spectra_*.png (scaling_report), paper_narrative/temp_spectra*, + B1/B2 figures (TODO, gap)

% =====================================================================
\section{Scaling Laws and the Data-Limited Regime}
\label{sec:scaling}
% (supports C5)
% - Real param accounting (NSPModel + ExpansionHeads; old plot_scaling constants were stale):
%   micro = 12.65M. Domain-Chinchilla anchor D_uniq/P ~= 0.54 — ~20x tighter than text's 20:1 -> the
%   ~20k-frame dataset is INFORMATION-LIMITED; compute-optimal models are an order of magnitude
%   smaller than text intuition. Frame the contribution as the QUANTITATIVE D/P constant for an
%   AR-token loss on a true PDE dataset (the qualitative U-shape is already established literature).
% - Per-tier shape from the CANONICAL N=128 sweep: sc341 beats the VQ floor (0.52-0.67, non-monotone,
%   best small); sc917 ~ties (best mid); sc1941 above floor (cleanest monotone, best big). Best-T
%   rises with scale count.
% - Below-floor EMD subsection (secondary finding): medium-sc341 s13/s18 rollout pixel-EMD goes BELOW
%   the VQ round-trip floor via time-varying mode-averaging; sc917 does not (open question).
%   NEEDS (P0-4): disambiguate the unreproduced medium-sc341-nsp-large DOUBLE-beat, or report only the
%   solid EMD-beat and drop the TKE-beat (TMLR claim-reduction is the sanctioned move here).
% FIG: scaling_tempopt_n128/best_temperature.png, scaling_tempopt_n128/scaling_emd.png,
%      scaling_tempopt_n128/scaling_tke.png  (these SUPERSEDE plots/scaling_tempopt/)

% =====================================================================
\section{Sampling Temperature: Climate vs Forecast Regimes}
\label{sec:temperature}
% (Hosts the recent inference-only sweeps — the delta since the work-log conversation.)
% - U-shaped EMD with a deployable optimum; collapse at BOTH extremes (cold-diffusive / hot-speckle),
%   stable in the middle (medium-sc917-s22: 100%/35%/0.8%/95%/100% collapse at T=1.2/1.4/1.6/1.8/2.0).
% - METRIC-HONESTY result: TKE/enstrophy RSE is GAMEABLE by broadband noise (keeps dropping past the
%   EMD optimum while the field mode-collapses) -> pick T by EMD + pixel PDF, never by spectral RSE.
% - Climate vs forecast: long-run optimum is WARM (anti-collapse), short-horizon optimum is COLD
%   (tracking). sc917 ~1.8 (climate) vs ~0.80 (forecast); sc1941 ~1.9 vs ~1.00. (Forecast optimum is
%   PINNED at the cold bracket edge -> reported divergence is a LOWER BOUND; un-pin to T=0.7.)
% NEEDS (P0-3): multi-seed confirmation of the optima (current points are noisy n_traj=1 on a U).
% FIG: scaling_forecast/forecast_best_temperature.png, scaling_forecast/forecast_vs_longterm_emd.png,
%      snapshots_n128_tempcollapse/medium-sc917-nsp-s22_temp_collapse.png

% =====================================================================
\section{Related Work}
\label{sec:related}
% Cluster (see OUTLINE.md Part G for the full grouping + the differentiation line for each):
% - Next-scale generation: VAR + image follow-ups (ZERO PDE/turbulence applications -> the gap).
% - Discrete tokenized PDE/physics AR: Zebra (raster), PhysiX (raster + refinement), Momenifar.
% - Continuous PDE-stability control (the contrast bucket): PDE-Refiner (PRIMARY foil), Stachenfeld
%   (training noise), Brandstetter MP-PDE (pushforward = our substitution-noise analog), Thermalizer.
% - Self-predictive / latent world models (future-work lineage): NextLat, Dreamer, DCWM, TECO, SPR,
%   TWISTER.
% - Scaling laws: Chinchilla, Henighan, Muennighoff (data-constrained), Practical-Scaling-Laws-on-PDE.

% =====================================================================
\section{Limitations and Open Questions}
\label{sec:limitations}
% - sc917 shows no below-floor effect (open). Single turbulence system (a 2nd dataset — the Dedalus
%   Rayleigh-Benard solver from gust v1 — is future work). Substitution rate only 0.1 ever tried.
%   The one remaining tuned knob (sampling T) — an entropy/typical/min-p sampler that LANDS at the
%   optimum without a sweep would close the "less intervention" pillar.
% - State the honesty caveats as limitations, not buried: floor is conditional/approximate; spectral
%   bias relocated-not-removed; EMD necessary-but-insufficient.

% =====================================================================
\section{Conclusion}
\label{sec:conclusion}
% Short. Restate the categorical (structural vs numerical) claim and what evidence backs it.

% --- Optional unnumbered end-matter (TMLR) -------------------------------------
% \section*{Broader Impact Statement}      % only REQUIRED if significant harm risk (turbulence
%                                           % surrogate is low-risk; a 2-3 line statement is fine).
% \section*{Author Contributions}          % CAMERA-READY ONLY (omit from anonymous submission).
% \section*{Acknowledgments}               % CAMERA-READY ONLY (omit from anonymous submission).

\bibliography{references}
\bibliographystyle{tmlr}

\appendix
% TMLR appendices are unbounded and read at reviewer discretion -> push the encyclopedic material here
% so the main text stays "justified by content."
\section{Hyperparameters and Training Configuration}      % VQ-VAE + NSP full tables
\section{Compute and Infrastructure}                      % Bridges2 H100 / Derecho A100, Slurm/PBS
\section{Full Scaling Sweep Tables}                        % all 9 VQ x 5 NSP cells, N=128
\section{Additional Rollout Grids and Spectra}             % extra snapshot/spectra grids
\section{Proposed Self-Predictive Auxiliary Loss (Future Work)}  % the NextLat-derived d-step bolt-on

\end{document}
